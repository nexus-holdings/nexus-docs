version: 2
updates:
  # Bump pinned GitHub Actions versions weekly
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    open-pull-requests-limit: 5
    groups:
      # One PR per week bundling all minor/patch actions bumps
      actions-minor:
        update-types: [minor, patch]
